# Ad Intelligence Pipeline — Dependencies (Cloud / Lightweight)
# Heavy ML models (torch, whisper, CLIP, BLIP, etc.) are LOCAL ONLY.
# Cloud mode uses only VLM APIs + OpenCV for frame extraction.

# Core
streamlit>=1.45.0
pydantic>=2.11.0
python-dotenv>=1.1.0

# Image processing (needed for frame extraction + color analysis)
Pillow>=11.0.0
opencv-python-headless>=4.11.0
numpy>=1.26.0
scikit-learn>=1.6.0

# Language Detection (tiny, no ML model)
langdetect>=1.0.9

# LLM APIs (the actual brain — all processing happens remotely)
groq>=0.25.0
google-genai>=1.14.0

# YouTube Download
yt-dlp>=2025.1.0

# HTTP
requests>=2.32.0

# RAM detection
psutil>=6.0.0